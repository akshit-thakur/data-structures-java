/**
 * LinkedList
 */
public class LinkedList {
    static class Node {
        int data;
        Node next;

        public Node(int d) {
            this.data = d;
            this.next = null;
        }
    }
}